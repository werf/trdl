{{- $env := .Values.werf.env }}
{{- $host := pluck $env .Values.host | first | default .Values.host._default }}
{{- if hasPrefix "review" $env }}
{{- $host = ( printf "%s.%s" $env (pluck "dev" .Values.host | first | default .Values.host._default ) | lower ) }}
{{- end }}
{{- $ingressClassName := pluck .Values.global.env .Values.ingressClassName | first | default .Values.ingressClassName._default | quote }}
{{- $targetCluster := include "targetCluster" . | trim }}
{{- $isProduction := eq .Values.global.env "production" }}
{{- $renderRU := or (not $isProduction) (eq $targetCluster "ru") }}
{{- $ruHost := include "ruHost" $host | trim }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tuf-router
spec:
  ingressClassName: {{ $ingressClassName }}
  tls:
  - hosts:
      - {{ $host }}
{{- if $renderRU }}
{{- if $isProduction }}
      - {{ $ruHost }}
{{- else }}
      - ru-{{ $host }}
{{- end }}
{{- end }}
{{- if $isProduction }}
    secretName: tls-trdl-dev
{{- else }}
    secretName: {{ pluck .Values.global.env .Values.ingressSecretName | first | default .Values.ingressSecretName._default }}
{{- end }}
  rules:
  - host: {{ $host }}
    http:
      paths:
      - path: /targets
        pathType: Prefix
        backend:
          service:
            name: tuf-router
            port:
              name: http
      - path: /download
        pathType: Prefix
        backend:
          service:
            name: tuf-router
            port:
              name: http
{{- if $renderRU }}
{{- if $isProduction }}
  - host: {{ $ruHost }}
{{- else }}
  - host: ru-{{ $host }}
{{- end }}
    http:
      paths:
      - path: /targets
        pathType: Prefix
        backend:
          service:
            name: tuf-router
            port:
              name: http
      - path: /download
        pathType: Prefix
        backend:
          service:
            name: tuf-router
            port:
              name: http
{{- end }}

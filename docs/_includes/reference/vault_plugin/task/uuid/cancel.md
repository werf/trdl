## `/task/:uuid/cancel`

Cancel running task

### Cancel running task



| Method | Path |
|--------|------|
| `POST` | `/task/:uuid/cancel` |

#### Parameters

* `uuid` (`string: <required>`, url param) — Task UUID.

#### Responses

* 200 — OK.
